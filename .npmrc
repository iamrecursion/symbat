# Obsidian finds a plugin release by looking for a git tag identical to the
# `version` field in manifest.json — `1.0.0`, never `v1.0.0`. npm tags with a
# leading `v` by default, which would publish a release Obsidian cannot see.
tag-version-prefix=
